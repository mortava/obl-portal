
● Plan delivered. Summary:

  Phase 1 — First sign-in flow (Low, ~30 min)
  Doc runbook + a Postgres trigger that auto-creates a profiles row on auth.users insert + a SQL snippet to self-elevate the first
  admin.

  Phase 2 — Real backend tables (High, 6–8 hr)
  11 tables (tenants, profiles, workflows, runs, run_steps, connections, connection_health_checks, audit_entries, alerts, policies,
  policy_layers) under supabase/migrations/0001-0003*.sql, RLS keyed off a is_platform_admin() SQL function, generated TS types in
  lib/database.types.ts, a lib/data/* data-access layer with a DEV_FALLBACK flag, and conversion of platform pages from
  client+sample-arrays to RSC fetches.

  Phase 3 — /platform/* RBAC (Medium, 2–3 hr, depends on Phase 2)
  requirePlatformAdmin() server helper, /403 page, middleware role check cached in a signed obl-role cookie (no per-page DB query),
  layout-level defense in depth, vitest + Playwright tests.

  Phase 4 — PROD_URL CI variable (Low, 5 min, ship today)
  One gh variable set PROD_URL --repo mortava/obl-portal --body "<alias>", then a trigger commit.

  Recommended rollout: Phase 4 today → Phase 1 docs today → Phase 2 next block → Phase 3 after Phase 2 lands.

  WAITING FOR CONFIRMATION: Proceed with this plan? (yes / modify / skip phase X)